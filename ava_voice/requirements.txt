# Ava Voice — foundation dependencies (Python 3.11)
# Pinned to compatible minimum versions; adjust as the project evolves.

# --- Web framework & server ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
websockets>=12.0,<13.0

# --- Configuration & validation ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

# --- Cache (Redis-ready; optional at runtime) ---
redis>=5.0,<6.0

# --- ML runtime ---
torch>=2.2,<3.0

# --- Development & testing ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
